<h1>New song page</h1>
<h1>Add A Song To the Billboard: <%= @bboard.genre %> </h1>

	<% @songs.each do |song| %>
	  <li>
	    <%= link_to song.name, add_bboard_song_path(@bboard, msong), method: :post %>
	  </li>
	  <hr />
	<% end %>


<%= link_to 'Back', bboards_path%>